rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Keypad rooms store only client-side AES-GCM ciphertext.
  match /rooms/{roomId} {
      allow read, write: if true;
    }
  }
}
